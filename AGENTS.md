# Agent rules

- Mandatory: preserve the offline-conversion contract—canonical `leadUuid`
  join, legacy `leadId` alias only, bearer `CRM_CALLBACK_SECRET`, exact stage
  mappings and defaults, stored original attribution, `external_id` /
  `event_id` retry deduplication, and explicit positive `Purchase` value.
  Never add a universal sale value.
